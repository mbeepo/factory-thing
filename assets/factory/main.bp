let iron = Iron();
let copper = Copper();
let copperWire = CopperWire(copper);
let electromagnets = Electromagnet(iron, copperWire) * 10;

electromagnets.solve();
electromagnets.buffer(p_electromagnet, 256);
copperWire.buffer(p_copper_wire, 256);
copper.buffer(p_copper, 256);
iron.buffer(p_iron, 256);